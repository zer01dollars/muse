import { StudioApp } from "@/components/studio/StudioApp";

export const metadata = {
  title: "Studio — Muse",
  description: "Build and refine your Muse digital twin.",
};

export default function StudioPage() {
  return <StudioApp />;
}
